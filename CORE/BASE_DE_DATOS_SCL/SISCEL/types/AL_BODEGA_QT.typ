CREATE OR REPLACE TYPE AL_BODEGA_QT AS OBJECT
(
  COD_BODEGA NUMBER(6),
  DES_BODEGA VARCHAR2(30),
  TIP_BODEGA NUMBER(2),
  COD_DIRECCION NUMBER(8),
  IND_ASIGNADA NUMBER(1),
  COD_BODEGA_PADRE NUMBER(6),
  NUM_TELEFONO1 VARCHAR2(15),
  NUM_TELEFONO2 VARCHAR2(15),
  NUM_FAX VARCHAR2(15),
  NOM_RESPONSABLE VARCHAR2(40),
  COD_GRPCONCEPTO NUMBER(6),
  CONSTRUCTOR FUNCTION AL_BODEGA_QT RETURN self AS result
)
/
SHOW ERRORS
CREATE OR REPLACE TYPE BODY AL_BODEGA_QT IS

  CONSTRUCTOR FUNCTION AL_BODEGA_QT RETURN self AS result AS
  BEGIN
    COD_BODEGA := NULL;
    DES_BODEGA := NULL;
    TIP_BODEGA := NULL;
    COD_DIRECCION := NULL;
    IND_ASIGNADA := NULL;
    COD_BODEGA_PADRE := NULL;
    NUM_TELEFONO1 := NULL;
    NUM_TELEFONO2 := NULL;
    NUM_FAX := NULL;
    NOM_RESPONSABLE := NULL;
    COD_GRPCONCEPTO := NULL;
    RETURN;
  END;
END;
/
SHOW ERRORS
