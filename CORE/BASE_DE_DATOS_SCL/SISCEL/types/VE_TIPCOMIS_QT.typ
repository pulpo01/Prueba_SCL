CREATE OR REPLACE TYPE VE_TIPCOMIS_QT AS OBJECT
(
  COD_TIPCOMIS     VARCHAR2(2),
  DES_TIPCOMIS     VARCHAR2(30),
  IND_CLIENTE      NUMBER(1),
  IND_BODEGA       NUMBER(1),
  IND_VTA_EXTERNA  NUMBER(1),
  IND_RIESGO       NUMBER(1),
  FEC_ULTMOD       DATE,
  NOM_USUARIO      VARCHAR2(30),
  IND_PRIVILEGIO   NUMBER(1),
  CONSTRUCTOR FUNCTION VE_TIPCOMIS_QT RETURN self AS result
)
/
SHOW ERRORS
CREATE OR REPLACE TYPE BODY VE_TIPCOMIS_QT IS

  CONSTRUCTOR FUNCTION VE_TIPCOMIS_QT RETURN self AS result AS
  BEGIN
  COD_TIPCOMIS := NULL;
  DES_TIPCOMIS := NULL;
  IND_CLIENTE := NULL;
  IND_BODEGA := NULL;
  IND_VTA_EXTERNA := NULL;
  IND_RIESGO := NULL;
  FEC_ULTMOD := NULL;
  NOM_USUARIO := NULL;
  IND_PRIVILEGIO := NULL;
    RETURN;
  END;
END;
/
SHOW ERRORS
