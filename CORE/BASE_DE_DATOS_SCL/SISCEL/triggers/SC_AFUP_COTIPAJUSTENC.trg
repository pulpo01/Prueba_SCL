CREATE OR REPLACE TRIGGER SC_AFUP_COTIPAJUSTENC
AFTER UPDATE
ON CO_TIPAJUSTENC FOR EACH ROW

WHEN (
NEW.DES_TIPAJUSTENC <> OLD.DES_TIPAJUSTENC
      )
DECLARE
v_codigo NUMBER(2);
BEGIN
       SELECT cod_dominio_cto
       INTO v_codigo
       FROM SC_DOMINIO_CTO
       WHERE DES_DOMINIO_CTO = 'CO_TIPAJUSTENC';
       IF v_codigo > 0 THEN
       UPDATE SC_CONCEPTO
       SET DES_CONCEPTO = :NEW.DES_TIPAJUSTENC
       WHERE COD_DOMINIO_CTO = v_codigo
       AND COD_CONCEPTO = SUBSTR(TO_CHAR(:OLD.COD_TIPAJUSTENC,'099999'),2,6);
       END IF;
EXCEPTION
       WHEN NO_DATA_FOUND THEN
                 v_codigo := 0;
       WHEN OTHERS THEN
                 RAISE_APPLICATION_ERROR  (-20002,'ERROR INESPERADO EN TRIGGER : ORA-'||TO_CHAR(SQLCODE)||'.',TRUE);
END;
/
SHOW ERRORS
